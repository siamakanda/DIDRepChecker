# Core async scraping
aiohttp>=3.8.0
lxml>=4.9.0
aiosqlite>=0.19.0

# CLI and rich output
rich>=13.0.0
tqdm>=4.65.0
pyperclip>=1.8.0

# API client
requests>=2.31.0

# API server (FastAPI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# For production with Gunicorn (optional, but deploy script uses it)
gunicorn>=20.1.0