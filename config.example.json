{
    "_comment": "Copy this to config.json and adjust values. All fields are optional — defaults are shown.",
    "cache_ttl_days": 3,
    "concurrent_requests": 30,
    "requests_per_second": 5,
    "max_retries": 2,
    "timeout": 15,
    "api_host": "0.0.0.0",
    "api_port": 8000,
    "api_key_required": true,
    "allowed_api_keys": ["YOUR_API_KEY_HERE"]
}
